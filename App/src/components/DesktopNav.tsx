import { Button } from "./ui/button"

const DesktopNav = () => {
  return (
    <Button variant={"ghost"} className="font-bold text-base hover:text-orange-500 hover:bg-white">
        Login
    </Button>
  )
}

export default DesktopNav